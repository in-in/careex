mixin button(opts={})

  - const className = 'button'

  mj-button(
    css-class=opts.cssClass ? className + ' ' + opts.cssClass : className
    href=opts.href || '#0')&attributes(attributes)= opts.text